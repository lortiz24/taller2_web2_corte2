/* import createDoscDatabase from '../../helpers/createDoscDatabase'

describe('Pruebas en el helper getGifs', () => {

    test('debe traer 5 elementos por defecto', async() => {
       const resul=await createDoscDatabase('simpsons');
       expect(resul).toBe(5);
    });
    test('debe traer 10 elementos como parametro', async() => {
       const gifs=await createDoscDatabase('simpsons',10);
       expect(gifs.length).toBe(10);
    });
    



}) */